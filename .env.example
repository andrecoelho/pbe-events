# Database Configuration
# PostgreSQL connection string
# Bun automatically handles connection pooling (default pool size: 10)
# To customize pool size, add query parameter: ?pool_size=20
DATABASE_URL=postgresql://user:password@localhost:5432/pbe_events

# Server Configuration
NODE_ENV=development
